import { createAction } from "redux-act";

export const changebox = createAction("change box event");
export const corPage = createAction("correction page event");
export const addname = createAction("Add name event");
export const addsurname = createAction("Add surname event");
